name: code
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.4
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
#version: 1.5.1+10142000
version: 1.3.7+704211630

environment:
  sdk: '>=3.3.0 <4.0.0'

# Dependencies specify other packages that your package needs in order to work.

# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter



  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.6
  provider: ^6.1.2
  tt_indicator: ^0.0.2
  flutter_reactive_ble: ^5.3.1
  uuid: ^4.3.3
  camera:  ^0.10.5+9
  mobkit_dashed_border: ^0.0.6
  flutter_easyloading: ^3.0.5
  path: ^1.8.1
  path_provider: ^2.1.2
  video_player: ^2.8.2
  permission_handler: ^11.3.0
  google_sign_in: ^6.2.1
  dio: ^5.4.2+1
  json_serializable: ^6.7.1
  json_annotation: ^4.8.1
  shared_preferences: ^2.2.2
  sign_in_with_apple: ^7.0.1
  date_format: ^2.0.7
  video_thumbnail: ^0.5.3
  sqflite: ^2.3.2
  flutter_screenutil: ^5.9.0
  syncfusion_flutter_charts: ^28.2.6
  get_it: ^8.0.3
  flutter_cupertino_datetime_picker: ^3.0.0
#  firebase_messaging: ^14.9.4
#下面的代码解决这个问题Error (Xcode): ../../../../.pub-cache/hosted/pub.dev/win32-5.5.0/lib/src/guid.dart:32:9: Error: Type 'UnmodifiableUint8ListView' not found.
  win32: ^5.5.4
  share_plus: ^10.0.0
  country_picker: ^2.0.27
  vibration: ^3.1.3
  image_picker: ^1.1.0
  chewie: ^1.8.1
  auto_size_text: ^3.0.0
  image_cropper: ^8.0.1
  responsive_grid_list: ^1.4.0
  flutter_to_airplay: ^2.0.4
  flutter_app_badger: ^1.5.0
  firebase_analytics: ^11.3.0
  firebase_auth: ^5.2.0
  firebase_messaging: ^15.1.0
  firebase_core: ^3.4.0
  package_info_plus: ^8.0.0
  device_info_plus: ^10.1.0
  cached_network_image: ^3.3.1
  wakelock_plus: ^1.2.5
  webview_flutter: ^4.8.0
#  status_bar_control: ^3.2.1
  in_app_purchase: ^3.2.0
  audioplayers: ^6.1.0
  switcher_button: ^0.0.4
  drop_down_list: ^2.0.0
  animated_custom_dropdown: ^3.1.1
  native_device_orientation: ^2.0.3
  widget_tooltip: ^1.0.5
  gif: ^2.3.0
  video_compress: ^3.1.4
  flutter_app_badge: ^2.0.2
  open_settings_plus: ^0.4.0
  dropdown_button2: ^2.3.9
  url_launcher: ^6.2.5
dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  build_runner: ^2.4.9
# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - images/bottom/
    - images/participants/
    - images/account/
    - images/base/
    - images/ble/
    - images/airbattle/
    - images/ranking/
    - images/profile/
    - images/product/
    - images/product//scene1/model1/
    - images/product//scene1/model2/
    - images/product//scene1/model3/
    - images/product//scene1/model4/
    - images/product//scene1/model5/
    - images/product//scene1/model6/
    - images/product//scene1/model7/
    - images/product//scene1/model8/
    - images/launch/
    - images/battery/
    - images/p3/
    - images/razor/
    - images/razor/mode/
    - images/razor/progress/
    - images/razor/progress/1/
    - images/razor/progress/1/static/
    - images/razor/progress/2/
    - images/razor/progress/2/static/
    - images/razor/progress/3/
    - images/razor/progress/3/static/
    - images/razor/progress/4/
    - images/razor/progress/4/static/
    - images/razor/progress/5/
    - images/razor/progress/5/static/
    - images/razor/progress/6/
    - images/razor/progress/6/static/
    - images/razor/progress/7/
    - images/razor/progress/7/static/
    - images/razor/progress/8/
    - images/razor/progress/8/static/
    - assets/audio/
    - assets/video/
    - images/subscribe/
    - images/stats/


  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/assets-and-images/#from-packages

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in flutterthis list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:

  fonts:
    - family: SanFranciscoDisplay
      fonts:
        - asset: fonts/SanFranciscoDisplay-Black.ttf
        - asset: fonts/SanFranciscoDisplay-Bold.ttf
        - asset: fonts/SanFranciscoDisplay-Medium.ttf
        - asset: fonts/SanFranciscoDisplay-Regular.ttf
    - family: DS-DIGI
      fonts:
        - asset: fonts/DS-DIGI.ttf
    - family: BebasNeue
      fonts:
        - asset: fonts/BebasNeue-Regular.ttf
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
